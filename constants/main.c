#include <stdlib.h>
#include <stdio.h>

int main() {
    const int NUMBER = 5;
    printf("Number = %d\n", NUMBER);
    const int RESULT = NUMBER + 5;
    printf("Result = %d\n", RESULT);
    return EXIT_SUCCESS;
}